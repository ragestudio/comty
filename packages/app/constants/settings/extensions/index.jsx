import React from "react"
import loadable from "@loadable/component"

export default {
    id: "extensions",
    icon: "MdOutlineCode",
    label: "Extensions",
    group: "advanced",
    settings: [

    ]
}