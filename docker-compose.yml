version: "3"

services:
  app:
    build: packages/app
    restart: unless-stopped
    ports:
      - "9000:9000"
  api:
    build: packages/api
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "3001:3001"