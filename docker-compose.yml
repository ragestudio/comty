version: "3"

services:
  app:
    build: packages/wrapper
    restart: unless-stopped
    ports:
      - "9000:9000"
  api:
    build: packages/server
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env